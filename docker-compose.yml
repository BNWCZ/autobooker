services:
  joordames:
    build: .
    env_file: .env
    volumes:
      - ./session:/app/session
      - ./bookings.csv:/app/bookings.csv
      - ./service_account.json:/app/service_account.json:ro
      - ./debug:/app/debug
